type Props = {};
import Footer from './Footer';
import Header from './Header';
const Layout = (props: Props) => {
  return (
    <>
      <Header />
      body
      <Footer />
    </>
  );
};

export default Layout;
